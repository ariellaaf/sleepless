<h1>Listing sessions</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @sessions.each do |session| %>
    <tr>
      <td><%= session.title %></td>
      <td><%= session.text %></td>
      <td><%= link_to 'Search', session_path(session) %></td>
    </tr>
  <% end %>
</table>